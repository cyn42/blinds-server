/**
 * config.h
 *
 * Contains sensitive and environment specific info that should not
 * be checked into source control.
 *
 * Rename to 'config.h' and add your environment specific info
 *
 */

#define WIFISSID "SSID"
#define WIFIPASSWORD "PASSWORD"
#define IP { 192, 168, 100, 127 }
#define GATEWAY { 192, 168, 100, 1 }
#define SUBNET { 255, 255, 255, 0 }
